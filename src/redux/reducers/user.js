
import { PROFILE_PICTURE } from "../types"

initialstate={
    fileInputState,
    selectedFile,
}
const userProfile=(state,action)=>{

    switch(action.type){
        case 'PROFILE_PICTURE':
              return 
    }

}